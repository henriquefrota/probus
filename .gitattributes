# Normalize line endings to LF in the repository.
# Git will convert CRLF → LF on commit and leave LF on checkout.
* text=auto eol=lf

# Binary files — never touch line endings.
*.png binary
*.jpg binary
*.whl binary
